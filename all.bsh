#!/bin/bash

source $(dirname $0)/np2r.env

services=(postgresql rabbitmq-server celeryd flower httpd notebook)

for s in "${services[@]}"
do
  echo -en "\\033[1;31m"
  echo -n "Running $1 $s:"
  echo -e "\\033[0;39m"
  ${INIT_DIR}/$s "${@}"
  echo
done
