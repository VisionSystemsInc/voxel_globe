#!/bin/bash

. ../np2r.env

export PYTHONPATH=${PYTHONPATH:+${PYTHONPATH}:}${CELERY_PROCESSORS}

os=`uname`

if [ "${os:0:5}" == "CYGWI" ]
then
  python.exe manage.py "${@}"
else
  python manage.py "${@}"
fi
