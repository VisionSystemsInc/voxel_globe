
PGENGINE=${NPR_ROAM_DIR}
PGPORT=${NPR_POSTGRESQL_PORT}
PGDATA=${NPR_POSTGRESQL_DATABASE}
PGLOG=${NPR_POSTGRESQL_LOG}
# Log file for pg_upgrade
PGUPLOG=${NPR_POSTGRESQL_LOG_DIR}/upgrade.log

pidfile=${NPR_POSTGRESQL_PID_DIR}/postgresql.pid
lockfile=${NPR_POSTGRESQL_LOCK_DIR}/postgresql

PG_USER=${NPR_POSTGRESQL_DAEMON_USER}
PG_GROUP=${NPR_DAEMON_GROUP}

PGOPTS=

#export POSTGIS_ENABLE_OUTDB_RASTERS=1
#export POSTGIS_GDAL_ENABLED_DRIVERS=ENABLE_ALL
#export POSTGIS_GDAL_ENABLED_DRIVERS="GTiff PNG JPEG GIF XYZ"

PG_START_TIMEOUT=10

PG_AUTH=${NPG_POSTGRESQL_AUTH}

if [ $(id -u) -ne 0 ]; then
  PG_USER=`id -u -n`
  PG_GROUP=`id -g -n`
fi
