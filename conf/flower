PID_FILE="${CELERY_PID_DIR}/flower.pid"
LOCK_FILE="${CELERY_LOCK_DIR}/flower"

PYTHON_BIN="${ROAM_DIR}/python"
FLOWER_BIN="${BINDIR}/flower"

FLOWER_USER=${CELERY_USER}
FLOWER_GROUP=${DAEMON_GROUP}
FLOWER_LOG_DIR=${CELERY_LOG_DIR}
