#!/bin/bash

source npr.env $1

kill -INT `head -n 1 ${DATABASE_DIR}/postmaster.pid`
#Normal kill will wait for everyone to disconnect
#-15 would just kill it outright
#-INT at least shuts everything down properly


